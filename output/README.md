# output

Part of `template_autopoiesis` (see its README for context).

Pipeline output tree generated by the repo's canonical pipeline stages (working outputs; regenerable).

Child folders: `children/`, `data/`, `figures/`, `manuscript/`, `pdf/`, `reports/`, `web/`

These are pipeline-generated working outputs; regenerate via the enclosing
repo's documented pipeline rather than editing by hand. Docs auto-created
2026-08-29 by the ongoing-docs fleet pass.
