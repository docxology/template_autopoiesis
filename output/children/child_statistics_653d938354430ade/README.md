# child_statistics_653d938354430ade

Part of `template_autopoiesis/output/children` (see its README for context).

Subtree of the enclosing project; role inferred from sibling convention (unverified).

Child folders: `manuscript/`, `primitives/`, `tests/`
Files: `analysis.py`, `figures.py`, `provenance.json`, `pyproject.toml`, `seal.json`

These are pipeline-generated working outputs; regenerate via the enclosing
repo's documented pipeline rather than editing by hand. Docs auto-created
2026-08-29 by the ongoing-docs fleet pass.
